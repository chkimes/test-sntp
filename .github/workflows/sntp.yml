name: Test SNTP
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  sntp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Use sntp client
        run: |
          sudo tcpdump -i any -nn port 123 &
          sudo apt install sntp
          sudo sntp -4 time.google.com || true
          pgrep sntp
